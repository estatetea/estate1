self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={body:event.data?.text()||''}};event.waitUntil(self.registration.showNotification(data.title||'Aegis',{body:data.body||'Estate Tea AI needs your attention.',icon:'/agents/sm_aegis.jpg',badge:'/agents/sm_aegis.jpg',tag:data.tag||'aegis-alert',renotify:!!data.renotify,data:{url:data.url||'/ai'}}))});self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/ai';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus'in c){c.navigate(url);return c.focus()}}return clients.openWindow(url)}))});